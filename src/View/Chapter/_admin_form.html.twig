{# TODO : manage errors display #}


<form method="post">
	<input type="hidden" value="{{ chapter.id }}" name="id">
	<p>
		<label for="name">Nom</label>
		<input type="text" value="{{ chapter.name }}" name="name" id="name">
	</p>
	<p>
		<label for="name">Titre</label>
		<input type="text" value="{{ chapter.title }}" name="title" id="title">
	</p>
	<p>
		<label for="name">Description</label>
		<input type="text" value="{{ chapter.description }}" name="description" id="description">
	</p>
	<p>
		<label for="name">Nom de l'image</label>
		<input type="text" value="{{ chapter.background_image }}" name="background_image" id="background_image">
	</p>
	<p>
		<label for="name">Balise alt de l'image</label>
		<input type="text" value="{{ chapter.background_image_alt }}" name="background_image_alt" id="background_image_alt">
	</p>
	<p>
		<button>Ajouter le chapitre</button>
	</p>
</form>
<p>Message à l'administrateur : bien penser à enregistrer le fichier image dans :
				    public/assets/images/</p>
<p>Saisir dans le formulaire le bon nom de fichier.</p>
